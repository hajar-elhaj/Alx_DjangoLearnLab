This API uses Django REST Framework generic views to handle CRUD operations
for the Book model.

Permissions:
- List and Detail views are public.
- Create, Update, and Delete views require authentication.

Generic views are used to reduce boilerplate and ensure clean RESTful design.
